<h2><%= @wiki_template.name %></h2>

<%= labelled_form_for @wiki_template, :html => {:multipart => false} do |f| %>
<%= render :partial => 'form', :locals => {:f => f} %>
<%= submit_tag l(:button_save) %>
<% end %>
